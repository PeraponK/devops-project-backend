name: test
on: 
  pull_request:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3

      - name: check node version # maybe already has node that come with ubuntu
        run: node -v

      - name: install dep
        run: npm ci
      
      - name: run test case
        run: npm test

  build-push:
    permissions:
      contents: read
      packages: write
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: checkout code
        uses: actions/checkout@v3

      # - name: github token
      #   uses: 
      - name: image name
        uses: echo "IMAGE_NAME=${{ github.repository }}" | tr '[:upper:]' '[:lower:]'

      - name: build image
        run: docker build -t IMAGE_NAME:${{ github.sha }} .

      - name: push image to ghcr
        run: docker push IMAGE_NAME:${{ github.sha }}
      
    #  ${{ github.repository }}
    #  ${{ github.sha }}
